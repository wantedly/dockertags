run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  disable:
    - golint
    - typecheck
    - structcheck
  enable:
    - errcheck
    - goimports
    - gocritic
    - govet
    - staticcheck

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
